package com.example.presentation.util

object Constants {
    const val ARG_HABIT = "habit"
    const val ARG_HABIT_TYPE = "habitType"
    const val KEY_UI_STATE = "ui_state"
    const val ARG_HABIT_ID = "habitId"

    // Константы для типов элементов списка (HabitAdapter)
    const val TYPE_SECTION_HEADER = 0    // Заголовок секции
    const val TYPE_HABIT_ITEM = 1        // Элемент привычки
}
